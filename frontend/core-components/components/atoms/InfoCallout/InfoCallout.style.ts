import { FontSizes } from '@fluentui/react/lib/Styling';
import { COLORS } from '../../../constants/Colors';

export const infoCalloutStyles = {
    root: {},
};

export const infoCalloutIconStyles = {
    root: {
        color: COLORS.darkGray160,
        fontSize: FontSizes.size12,
        paddingTop: 2,
    },
};

export const infoCalloutButtonStyles = {
    root: {
        width: 16,
        height: 16,
    },
};
