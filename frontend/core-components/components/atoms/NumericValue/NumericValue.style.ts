import { ITextStyles } from '@fluentui/react/lib/components/Text/Text.types';
import { COLORS } from '../../../constants/Colors';

export const numericValueStyles: ITextStyles = {
    root: {
        color: COLORS.primaryTagText,
    },
};
