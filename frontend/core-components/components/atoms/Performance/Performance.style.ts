import { COLORS } from '../../../constants/Colors';

export const PerformanceUpStyles = {
    root: {
        color: COLORS.successIcon,
        textColor: COLORS.successIcon,
        textAlign: 'left',
    },
};

export const PerformanceDownStyles = {
    root: {
        color: COLORS.red,
        textColor: COLORS.red,
        textAlign: 'left',
    },
};

export const PerformanceFlatStyles = {
    root: {
        color: COLORS.primaryTagText,
        textAlign: 'left',
    },
};
