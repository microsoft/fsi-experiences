import { createClassSelectorRange } from '../../../utilities/responsive/ResponsiveUtil';

export const separatedListPrefix = 'separated-list';

export const minFourColumns = createClassSelectorRange(4, 12, separatedListPrefix);

export const minSevenColumns = createClassSelectorRange(7, 12, separatedListPrefix);
