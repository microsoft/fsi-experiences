import { FontSizes } from '@fluentui/react/lib/Theme';

export const topicChoicesListStyles = {
    root: {},
};

export const groupOptionsStyle = { root: {}, label: { fontSize: FontSizes.size20, marginBottom: '40px', textAlign: 'start' } };
