import { minSixColumns } from '../../consts/reponsive.consts';

export const contentStyles = { root: { textAlign: 'start', [minSixColumns]: { flexDirection: 'row' } } };

export const contentTokens = { padding: 24, childrenGap: 24 };
