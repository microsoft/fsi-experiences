import { createClassSelectorRange } from '../../../../utilities/responsive/ResponsiveUtil';

export const customerIndicatorsPrefix = 'customer-indicators';

export const minFourColumns = createClassSelectorRange(4, 12, customerIndicatorsPrefix);

export const minSevenColumns = createClassSelectorRange(7, 12, customerIndicatorsPrefix);
