import { FontSizes, FontWeights } from '@fluentui/react';
import { COLORS } from '@fsi/core-components/dist/constants/Colors';

export const bankingCardTooltipBoxValueSize = {
    root: { fontSize: FontSizes.size12, fontWeight: FontWeights.regular, color: COLORS.black, lineHeight: 16 },
};
export const bankingCardTooltipTokens = { padding: 4, childrenGap: 10 };
