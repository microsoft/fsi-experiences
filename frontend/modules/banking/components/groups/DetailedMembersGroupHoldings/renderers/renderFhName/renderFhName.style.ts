import { FontSizes, FontWeights } from '@fluentui/react/lib/Styling';
import { COLORS } from '@fsi/core-components/dist/constants/Colors';

export const fhGroupNameStyles = {
    root: { fontSize: FontSizes.size14, fontWeight: FontWeights.regular, verticalAlign: '-webkit-baseline-middle', whiteSpace: 'normal' },
};

export const fhCompactTextStyles = {
    root: { fontSize: FontSizes.size12, fontWeight: FontWeights.regular, color: COLORS.darkGray, whiteSpace: 'break-spaces' },
};
