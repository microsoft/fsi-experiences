import { FontSizes, FontWeights, NeutralColors } from '@fluentui/react/lib/Theme';

export const groupMembertooltipHostStyle = {
    root: {
        display: 'inline-block',
        cursor: 'default',
        lineHeight: '14px',
    },
};

export const calloutProps = { gapSpace: 0 };

export const groupMembersTextStyle = {
    root: {
        fontSize: FontSizes.size20,
        fontWeight: FontWeights.regular,
    },
};

export const groupMembersSeparatorStyles = {
    root: {
        width: '100%',
    },
};

export const groupMembersPickerStyles = {
    root: {
        width: '100%',
        maxWidth: '320px',
    },
};

export const primaryTextStyles = { root: { fontWeight: FontWeights.semibold, color: NeutralColors.gray160, fontSize: FontSizes.size14 } };
