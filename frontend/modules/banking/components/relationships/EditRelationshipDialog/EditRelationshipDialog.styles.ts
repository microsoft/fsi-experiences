import { COLORS } from '@fsi/core-components/dist/constants';

export const headerStyles = {
    root: {
        '.ms-Suggestions-title': {
            color: COLORS.black,
        },
    },
};

export declare const editEventModalStyle: {
    main: {
        selectors: {
            '@media (min-width: 480px)': {
                maxWidth: number;
                width: number;
            };
        };
    };
};
