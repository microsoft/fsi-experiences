import { IButtonStyles } from '@fluentui/react';
import { IEmptyStateStyles } from '@fsi/core-components/dist/components/atoms/EmptyState';

export const noFileEmptyStateStyles: IEmptyStateStyles = {
    container: {
        paddingBlockEnd: 0,
    },
};

export const emptyUploadButtonStyles: IButtonStyles = {
    root: {
        marginBlock: 22,
    },
};
